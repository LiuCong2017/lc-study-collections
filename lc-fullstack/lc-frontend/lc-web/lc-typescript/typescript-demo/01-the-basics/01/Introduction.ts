/**
 *  A type-checker like TypeScript uses that information
 *  and tells us when things might be going off the rails.
 */
message.toLowerCase();
message();
const message = "hello";

function fn(x){
    return x.flip();
}

const user = {
    name: "kavin",
    age: 19,
};
user.location;

function greet(person: string,date: Date){
    console.log(`Hello ${person}, today is ${date.toDateString()}!`)
}
greet("kavin",new Date());
